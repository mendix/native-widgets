appId: "${APP_ID}"
---
- runFlow:
    file: "../../../../../../maestro/Precondition.yaml"
- retry:
    maxRetries: 2
    commands:
      - tapOn:
          text: "L"
      - extendedWaitUntil:
          visible: "Line chart"
          timeout: 10000
- tapOn:
    text: "Line chart"
- assertVisible:
    text: "Line chart"
- assertVisible:
    id: "container1"
# Wait for the screen to stop changing (image decode / sheet animation)
# rather than sleeping a fixed time: this returns as soon as the UI
# settles, and still caps at 15s.
- waitForAnimationToEnd:
    timeout: 15000
- takeScreenshot:
    path: "maestro/images/actual/${PLATFORM}/line_chart"
- assertScreenshot:
    path: "maestro/images/expected/${PLATFORM}/line_chart.png"
