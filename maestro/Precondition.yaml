appId: "${APP_ID}"
---
- retry:
    maxRetries: 3
    commands:
      - launchApp:
          clearState: true
          permissions:
            camera: allow
      - extendedWaitUntil:
          visible: "Widgets menu"
          timeout: 180000
